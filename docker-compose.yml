services:
  mongo:
    image: mongo:7
    container_name: listapp-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: listapp
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: